import 'package:flutter/material.dart';

class WebBaseData {
  final String? iconName;
  final Icon? icon;
  final Widget? screenName;
  final int? index;

  const WebBaseData({
    this.iconName,
    this.screenName,
    this.icon,
    this.index,
  });
}